{
  "name": "oli-kilt",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tsc": "tsc",
    "start": "ts-node-dev --respawn --transpile-only index.ts",
    "build": "tsc",
    "serve": "NODE_ENV=production node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@kiltprotocol/sdk-js": "dev",
    "python-shell": "^2.0.3",
    "typescript": "^4.1.3"
  },
  "devDependencies": {
    "ts-node-dev": "^1.1.1"
  }
}
